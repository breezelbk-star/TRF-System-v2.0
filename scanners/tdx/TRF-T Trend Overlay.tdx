{==============================
 TRF-T Trend Overlay Indicator
 可复制趋势股（10%-25%）
==============================}

MA10:MA(C,10),COLORWHITE;
MA20:MA(C,20),COLORYELLOW;
MA30:MA(C,30),COLORCYAN;

{--- Trend background condition ---}
TREND_OK:=C>MA30 AND MA30>=REF(MA30,1);

{--- "Background" hint (use bottom band instead of COLORRGB background) ---}
STICKLINE(TREND_OK,LLV(LOW,5)*0.995,LLV(LOW,5)*1.005,6,0),COLOR00CC66;

{==============================
 Signals (hint only)
==============================}

{1) Trend confirm (pullback ends / re-ignite)}
TREND_CONFIRM:=
    C>MA10
AND MA10>MA20
AND MA20>=MA30
AND REF(C,1)<=MA20;

DRAWICON(TREND_CONFIRM,LOW*0.98,1);
DRAWTEXT(TREND_CONFIRM,LOW*0.96,'TRF-T OK'),COLOR00CC66;

{2) Pullback watch zone (no buy, just watch)}
PULLBACK_ZONE:=
    C<=MA20*1.02
AND C>=MA30
AND MA30>=REF(MA30,1);

DRAWICON(PULLBACK_ZONE,LOW*0.99,2);

{3) Trend break (must exit)}
TREND_BREAK:=
    C<MA30
OR MA30<REF(MA30,1);

DRAWICON(TREND_BREAK,HIGH*1.02,15);
DRAWTEXT(TREND_BREAK,HIGH*1.04,'EXIT'),COLORRED;


# TRF-T Trend Overlay 使用说明（执行版）

> 本文件用于 **TRF-T 可复制趋势模型（10%–25%）** 的  
> **日线执行判断**，不用于选股，只用于「能不能动、什么时候撤」。

---

## 一、指标定位（非常重要）

- 本指标 = **执行过滤器**
- 不负责预测
- 不负责抄底
- 不给买卖价
- 只回答 3 个问题：

1. 当前是否允许做趋势？
2. 是否出现可以出手的结构？
3. 是否必须撤退？

---

## 二、指标构成说明

### 1️⃣ 均线系统
- MA10：节奏线
- MA20：回踩判断线
- MA30：**趋势生死线**

> **MA30 是否向上 = 是否允许做趋势**

---

### 2️⃣ 绿色背景（TREND_OK）

**出现条件：**
- 收盘价在 MA30 之上
- MA30 走平或向上

**含义：**
> 市场环境允许做趋势股

❌ 没有绿色背景 → 当天不做 TRF-T

---

### 3️⃣「TRF-T OK」信号（唯一主动信号）

**触发条件（全部满足）：**
- 收盘价重新站上 MA10
- MA10 > MA20 > MA30
- 前一交易日收盘在 MA20 下方（完成回踩）

**含义：**
> 回踩结束，趋势继续  
> **允许建立第一笔仓位**

---

### 4️⃣ 回踩观察区（图标 2）

**条件：**
- 价格在 MA20 附近
- 未跌破 MA30
- MA30 仍向上

**含义：**
> 趋势未坏，但不急

操作原则：
- 不加仓
- 不抄底
- 只观察

---

### 5️⃣ EXIT 信号（红字）

**触发任一即成立：**
- 收盘价跌破 MA30
- MA30 明确下拐

**含义：**
> 趋势破坏，无条件撤退

❌ 不等反抽  
❌ 不看基本面  
❌ 不找理由

---

## 三、完整执行流程（每天照做）

### ✅ 盘后 / 收盘后（5 分钟）

对每一只趋势股，只问 3 个问题：

| 问题 | 判断依据 | 动作 |
|---|---|---|
| 是否允许做 | 是否有绿色背景 | 否 → 放弃 |
| 是否能动 | 是否出现 TRF-T OK | 是 → 准备 |
| 是否必须走 | 是否出现 EXIT | 是 → 清仓 |

---

### ✅ 盘中执行原则

仅当：
- 处于绿色背景期
- 最近 1–3 日出现 TRF-T OK

才允许：
- 分时回踩观察
- 缩量 + 承接
- 顺势参与

否则：
> **关掉盘面，不操作**

---

## 四、适用标的范围

适合：
- 华海清科
- 北方华创
- 南大光电
- 中国卫星
- 同类型「非情绪主升」趋势股

不适合：
- 情绪连板
- 首板博弈
- 超跌反弹
- 侨源类极端主升（TRF-A）

---

## 五、核心口诀（必须记住）

> **绿底才看，OK 才动，EXIT 就走**

---

## 六、模型原则

- 不预测
- 不抄底
- 不幻想
- 只吃趋势中段的确定性

---

**文件归属：**
TRF-System / Trend-Playbook / TRF-T_Trend_Overlay_Guide.md
